let array = [ 1, 2, 3, false, null, 3];

//проверяем  наличие элемента с индексом 5
if (array[5] !== undefined) console.log(' Элемент есть');
if (array.length > 5) console.log(' Элемент есть');

// задаем пустое значение
let emptyVar = null;
